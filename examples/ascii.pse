declare i : integer
declare out : string
out <- ">"
for i <- 1 to 100
    out <- "-" & out
    if i mod 10 = 0 then
        out <- "\n   " & out
    endif
    output out
    system "sleep 0.1"
    system "clear"
next i


